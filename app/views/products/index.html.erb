<%= render 'layouts/menu_lateral_admin' %>
<legend>Productos</legend>
<%= link_to 'Nuevo producto', new_product_path,:class =>"btn btn-success btn-block" %>
<br>
<table class="table">
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @products.each do |product| %>
      <tr>
        <td><%= product.name %></td>
        <td><%= link_to 'Editar', edit_product_path(product) %></td>
        <td><%= link_to 'Destruir', product, method: :delete, data: { confirm: 'Estás seguro ?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= render 'layouts/footer_user' %>